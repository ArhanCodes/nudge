
<!-- claude-learn pack: typescript -->
## Project conventions (auto-added by claude-learn)

- TypeScript project — strict types preferred; avoid `any`.
- **Do not read `node_modules/` or `dist/` or `.next/` or `build/` ever**.
- When editing existing code, prefer `Edit` over `Write` for small changes.
- For imports, use relative paths unless a `paths` alias or `#imports` field is configured.
- Run the typechecker (`tsc --noEmit`) or linter before assuming code is correct.


<!-- claude-learn pack: nodejs -->
## Node.js project conventions (auto-added by claude-learn)

- **Do not read `node_modules/` ever** — tens of thousands of files, zero value.
- Check `package.json` scripts before suggesting commands; use those over raw `node`/`tsc` when available.
- Prefer `pnpm` if `pnpm-lock.yaml` exists, else `npm`, else `yarn`.


## Response style (auto-added by claude-learn)
- Be concise. No preamble. No summary at the end of every response.
- Use short sentences and bullet lists over paragraphs.
- Show code, not narration about the code.
- If nothing to add beyond the answer, stop writing.

## Project primer (auto-generated by claude-learn)
<!-- Refreshes when project structure changes. Helps Claude resume after context overflow without re-exploration. -->

### Structure
- `App.js` (file)
- `CLAUDE.md` (file)
- `README.md` (file)
- `app.json` (file)
- `assets` (dir)
- `babel.config.js` (file)
- `dist` (dir)
- `index.js` (file)
- `jest.config.js` (file)
- `node_modules` (dir)
- `package-lock.json` (file)
- `package.json` (file)
- `quill` (dir)

### Stack markers
- **Node/TS** (found `package.json`)

### Conventions detected
- (populated from claude-learn memory over time)

## Key file: Users/arhanharchandani/Downloads/linkedout/src/translator.ts
- **exports (fn):** `translate`
- **types:** `type Mode`
- **const/let:** `deflateExamples`, `inflateExamples`


## Inline summary: Users/arhanharchandani/Downloads/stark-hud/src/main.ts
- **depends on:** `@mediapipe/tasks-vision`


## Key file: Users/arhanharchandani/Downloads/stark-hud/src/gauntlet.ts
- **exports (fn):** `drawGauntlet`, `drawEditSkeleton`, `drawFloatingIndicator`


## Key file: Users/arhanharchandani/Downloads/stark-hud/src/style.css
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: quill/ui/components.jsx
- **depends on:** `react`, `react-native`
- **exports (fn):** `Screen`, `Card`, `Title`, `Muted`, `Button`, `Chip`, `ProgressRing`


## Key file: quill/screens/HomeScreen.jsx
- **depends on:** `react`, `react-native`
- **exports (fn):** `sortByDateDesc`, `pickGoalColor`, `pickBenchmarkBg`, `pickBenchmarkBorder`, `pickBenchmarkTextColor`, `pickLogColor`, `pickLogPrefix`, `HomeScreen`


## Key file: Users/arhanharchandani/Downloads/stark-hud/src/hands.ts
- **depends on:** `@mediapipe/tasks-vision`
- **exports (fn):** `setMirrorMode`, `processHand`, `assignRoles`
- **types:** `Vec2`, `HandState`, `type HandRole`, `HandsFrame`
- **const/let:** `LM`


## Inline summary: Users/arhanharchandani/Downloads/linkedout/src/translator.ts
- **exports (fn):** `translate`
- **types:** `type Mode`
- **const/let:** `deflateExamples`, `inflateExamples`


## Key file: Users/arhanharchandani/Downloads/stark-hud/index.html
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: Users/arhanharchandani/Downloads/stark-hud/src/config.ts
- **types:** `ColorTheme`, `type GauntletType`, `GauntletConfig`, `OptionDef`, `CategoryDef`, `Preset`
- **const/let:** `COLOR_THEMES`, `DEFAULT_CONFIG`, `GAUNTLET_TYPES`, `CATEGORIES`, `PRESETS`


## Key file: quill/App.quill
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: Users/arhanharchandani/Downloads/beam/src/main.rs
- **depends on crates:** `std`, `anyhow`, `clap`, `futures_util`, `tokio_tungstenite`, `tracing`
- **structs:** `Cli`
- **enums:** `Commands`
- **fn (top-level):** `format_size`


## Common command
`cd <PATH> && npx tsc --noEmit 2>&1` — used routinely in this project.

## Common command
`cd <PATH> && npm run build 2>&1` — used routinely in this project.

## Inline summary: quill/App.quill
<!-- Add a summary here to prevent re-reads. -->

## Inline summary: Users/arhanharchandani/Downloads/beam/src/main.rs
- **depends on crates:** `std`, `anyhow`, `clap`, `futures_util`, `tokio_tungstenite`, `tracing`
- **structs:** `Cli`
- **enums:** `Commands`
- **fn (top-level):** `format_size`


## Key file: Users/arhanharchandani/Downloads/beam/src/transfer.rs
- **depends on crates:** `std`, `anyhow`, `indicatif`, `quinn`, `sha2`, `tokio`, `tracing`
- **structs:** `SkipServerVerification`
- **fn (top-level):** `generate_self_signed_cert`, `make_server_config`, `make_client_config`, `verify_server_cert`, `verify_tls12_signature`, `verify_tls13_signature`, `supported_verify_schemes`, `format_size`


## Key file: quill/ui/components.quill
<!-- Add a one-paragraph summary of what this file exports/contains. -->

## Key file: src/ui/components.js
- **depends on:** `react`, `react-native`, `react-native-svg`
- **exports (fn):** `Screen`, `Card`, `Title`, `Muted`, `Button`, `Chip`, `ProgressRing`


## Key file: App.js
- **depends on:** `react`, `@react-navigation/native`, `@react-navigation/native-stack`, `expo-status-bar`
- **exports (fn):** `App`


## Key file: src/screens/HomeScreen.js
- **depends on:** `react`, `react-native`
- **exports (fn):** `HomeScreen`


## Inline summary: src/screens/HomeScreen.js
- **depends on:** `react`, `react-native`
- **exports (fn):** `HomeScreen`

